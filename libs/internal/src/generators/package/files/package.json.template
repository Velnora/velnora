{
  "name": "@velnora/<%= name %>",
  "version": "0.0.0-dev.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./build/velnora.<%= name %>.d.ts",
      "import": "./build/velnora.<%= name %>.mjs"
    }
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "unbuild",
    "lint": "eslint .",
    "stub": "unbuild --stub",
<% if (!skipTests) { %>    "test": "vitest run",
<% } %>    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "unbuild": "^3.3.1"
  },
  "engines": {
    "node": ">=24"
  },
  "publishConfig": {
    "access": "public"
  }
}
