/**
 * @velnora-meta
 * type: author
 * author: MDReal
 */

/**
 * Environment-specific overrides for a Velnora project.
 *
 * Keyed by environment name (e.g. `"dev"`, `"prod"`) inside
 * {@link VelnoraAppConfig.environments}.
 *
 * Currently a placeholder — will be populated as environment-aware
 * features (env variables, build flags, etc.) are implemented.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface VelnoraEnvironment {}
