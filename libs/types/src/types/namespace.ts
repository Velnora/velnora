/* eslint-disable @typescript-eslint/no-empty-object-type */
export {};

declare global {
  namespace Velnora {
    // what per-app config (defineAppConfig) can be extended with
    interface AppConfigExtensions {}
  }
}
